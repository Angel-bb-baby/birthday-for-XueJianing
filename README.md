# A little corner

A personal birthday website. Edit `src/data/content.js` to change the name, letter, memories, photos, timeline, and wishes.

Add photos to `public/photos/` using the filenames in `content.js`. Add `public/music/birthday.mp3` if you want the music button to appear.

```bash
npm install
npm run dev
npm run build
```
